Install:

```console
$ npm i
```

Run watch for build+server:

```console
$ npm run watch
```

Run watch for tests:

```console
$ npm run watch:test
```

Run build, tests, and start server once (without watch):

```console
$ npm run once
```
